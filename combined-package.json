{
  "name": "charette-system-combined",
  "version": "1.0.0",
  "description": "Combined architecture for Charette System (demo version)",
  "main": "combined-app.js",
  "scripts": {
    "start": "node combined-app.js",
    "build": "cd client && npm install && npm run build",
    "dev": "concurrently \"nodemon combined-app.js\" \"cd client && npm start\"",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
